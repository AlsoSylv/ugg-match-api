query FetchProfileRanks($regionId: String!, $summonerName: String!, $seasonId: Int!) {
  fetchProfileRanks(regionId: $regionId, summonerName: $summonerName, seasonId: $seasonId) {
    rankScores {
      lastUpdatedAt
      losses
      lp
      promoProgress
      queueType
      rank
      role
      tier
      seasonId
      wins
    }
  }
}